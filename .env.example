# Only needed for the optional --real / LLM07 system-prompt-leak probe
# (redteam/real_llm.py). Everything else - the CLI, the app, every test - runs
# with none of this set.
GEMINI_API_KEY=
REDTEAM_MODEL=gemini/gemini-1.5-flash
